{
  "name": "agentpm-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:setup": "wrangler d1 execute agentpm-registry --local --file=./schema.sql",
    "db:setup:prod": "wrangler d1 execute agentpm-registry --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.4.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.62.0"
  }
}
